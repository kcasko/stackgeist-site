<section class="featured" id="featured">
  <div class="divider"></div>
  <p class="eyebrow">Featured setup</p>
  <h2>Midnight Shift</h2>
  <p class="intro">A dark gaming + work setup built around atmosphere, usable desk space, and gear that earns its place.</p>

  <div class="feature-grid">
    <div class="setup-art" aria-label="Stylized dark desk setup illustration">
      <div class="ambient"></div>
      <div class="desk"></div>
      <div class="desk-shadow"></div>
      <div class="monitor"></div>
      <div class="stand"></div>
      <div class="stand-base"></div>
      <div class="speaker"><i></i><i></i></div>
      <div class="tower"></div>
      <div class="tower-light"></div>
      <div class="mat"></div>
      <div class="keyboard"></div>
    </div>

    <article class="feature-copy">
      <p class="eyebrow">Midnight Shift</p>
      <h3 class="desktop-title">Gaming + work</h3>
      <h3 class="mobile-title">Midnight Shift</h3>
      <p>Small-room friendly. Dark without turning the whole room into an RGB circus.</p>
      <dl>
        <div><dt>Good for</dt><dd>Gaming · work · console</dd></div>
      </dl>
      <a class="view-link" href="/setups/midnight-shift">View setup →</a>
    </article>
  </div>
</section>
<style>
  .featured {
    padding-top: 36px;
  }

  .featured > .eyebrow {
    margin-top: 32px;
  }

  h2 {
    margin: 16px 0 8px;
    font-size: 28px;
    letter-spacing: -0.035em;
  }

  .intro {
    max-width: 520px;
    margin: 0;
    color: var(--muted);
    font-size: 14px;
    line-height: 1.45;
  }

  .feature-grid {
    display: grid;
    grid-template-columns: 1.37fr 1fr;
    gap: 40px;
    margin-top: 22px;
  }

  .setup-art,
  .feature-copy {
    min-height: 300px;
    border: 1px solid var(--border);
    border-radius: 16px;
  }
  .setup-art {
    position: relative;
    overflow: hidden;
    background: linear-gradient(180deg, #0b0812, #20102e);
  }

  .ambient {
    position: absolute;
    left: 44%;
    top: 4%;
    width: 48%;
    aspect-ratio: 1.8;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(168, 85, 247, 0.28), transparent 68%);
    filter: blur(8px);
  }

  .desk {
    position: absolute;
    left: 12%;
    right: 12%;
    bottom: 24%;
    height: 14px;
    border-radius: 999px;
    background: #21182a;
  }

  .desk-shadow {
    position: absolute;
    left: 18%;
    right: 18%;
    bottom: 20%;
    height: 12px;
    border-radius: 4px;
    background: #110d18;
  }

  .monitor {
    position: absolute;
    left: 34%;
    top: 22%;
    width: 33%;
    height: 38%;
    border: 2px solid #5c347f;
    border-radius: 10px;
    background: linear-gradient(#12101b, #3a1452);
    box-shadow: 0 0 24px rgba(168, 85, 247, 0.16);
  }
  .stand {
    position: absolute;
    left: 48%;
    top: 61%;
    width: 7%;
    height: 10%;
    border-radius: 4px;
    background: #16101e;
  }

  .stand-base {
    position: absolute;
    left: 42%;
    top: 70%;
    width: 19%;
    height: 8px;
    border-radius: 4px;
    background: #21182a;
  }

  .speaker {
    position: absolute;
    left: 22%;
    bottom: 30%;
    width: 7%;
    height: 20%;
    display: grid;
    place-items: center;
    border: 1px solid #3b2a4c;
    border-radius: 8px;
    background: #0c0911;
  }

  .speaker i {
    width: 20px;
    height: 20px;
    border-radius: 50%;
    background: #28113b;
  }

  .tower {
    position: absolute;
    right: 20%;
    bottom: 31%;
    width: 6%;
    height: 30%;
    border: 1px solid #b9a5d0;
    border-radius: 10px;
    background: #ece8f2;
  }
  .tower-light {
    position: absolute;
    right: 21.7%;
    bottom: 34%;
    width: 2px;
    height: 24%;
    background: var(--purple);
  }

  .mat {
    position: absolute;
    left: 36%;
    bottom: 25.5%;
    width: 27%;
    height: 10px;
    border: 1px solid var(--border-strong);
    border-radius: 999px;
    background: #2a2033;
  }

  .keyboard {
    position: absolute;
    right: 26%;
    bottom: 26%;
    width: 8%;
    height: 8px;
    border-radius: 4px;
    background: #2a2033;
  }
  .feature-copy {
    padding: 28px;
    background: var(--surface);
  }

  .mobile-title { display: none; }

  .feature-copy h3 {
    margin: 20px 0 10px;
    font-size: 22px;
    letter-spacing: -0.03em;
  }

  .feature-copy > p:not(.eyebrow) {
    margin: 0;
    color: var(--muted);
    font-size: 14px;
    line-height: 1.5;
  }

  dl {
    margin: 24px 0 18px;
    padding-top: 18px;
    border-top: 1px solid var(--border);
  }

  dl div {
    display: grid;
    grid-template-columns: 100px 1fr;
    gap: 12px;
    margin-top: 12px;
  }
  dt {
    color: var(--quiet);
    font-size: 11px;
    font-weight: 700;
  }

  dd {
    margin: 0;
    color: var(--text);
    font-size: 13px;
    font-weight: 700;
  }

  .view-link {
    display: inline-flex;
    align-items: center;
    min-height: 36px;
    padding: 0 20px;
    border: 1.5px solid var(--purple);
    border-radius: 999px;
    background: var(--purple-deep);
    color: var(--purple-light);
    font-size: 12px;
    font-weight: 750;
    box-shadow: 0 0 10px rgba(168, 85, 247, 0.12);
  }

  .view-link:hover {
    background: var(--purple-active);
    box-shadow: 0 0 18px rgba(168, 85, 247, 0.22);
  }
  @media (max-width: 760px) {
    .featured {
      padding-top: 34px;
    }

    .featured > h2,
    .featured > .intro {
      display: none;
    }

    .feature-grid {
      grid-template-columns: 150px 1fr;
      gap: 20px;
      margin-top: 30px;
    }

    .setup-art,
    .feature-copy {
      min-height: 118px;
    }

    .feature-copy {
      padding: 4px 0;
      border: 0;
      background: transparent;
    }

    .feature-copy .eyebrow,
    .feature-copy > p,
    .feature-copy dl {
      display: none;
    }
    .desktop-title { display: none; }
    .mobile-title { display: block; margin: 0 0 8px; font-size: 16px; }

    .feature-copy::after {
      content: 'Gaming + work';
      white-space: pre-line;
      display: block;
      margin-bottom: 16px;
      color: var(--muted);
      font-size: 12px;
      line-height: 1.4;
    }

    .view-link {
      min-height: auto;
      padding: 0;
      border: 0;
      background: transparent;
      box-shadow: none;
      font-size: 12px;
    }

    .view-link:hover {
      background: transparent;
      box-shadow: none;
    }
  }
</style>
